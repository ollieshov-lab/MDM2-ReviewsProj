# Notebooks

Exploratory notebooks live here once they have a matching `.py` entrypoint for any tracked output they produce.

The migrated RP layout keeps:

- source logic in `code/lib/`
- canonical entrypoints in `code/pipelines/` or `code/figures/`
- notebooks reserved for exploration, presentation, and narrative inspection
