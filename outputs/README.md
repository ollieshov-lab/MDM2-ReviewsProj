# Outputs

`outputs/` is the canonical generated-results namespace for RP.

Expected subtrees:

- `models/`
- `plots/`
- `tables/`
- `interactive/`
- `intermediate/`
- `manifests/`

Tracked source files should not be written here except for documentation and manifest placeholders.
